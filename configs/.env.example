# =============================================================================
# QuantaAlpha environment configuration template
#
# Usage:
#   1. cp configs/.env.example .env
#   2. Edit paths and API Key below
#   3. ./run.sh "your exploration direction"
# =============================================================================

# =============================================================================
# Paths (core: adjust these for your environment)
# =============================================================================

# Qlib data directory (path after unpacking cn_data.zip; must contain calendars/, features/, instruments)
QLIB_DATA_DIR=<your_qlib_data_path>

# Output root (all results, cache, logs under this directory)
DATA_RESULTS_DIR=<your_results_path>

# =============================================================================
# Qlib
# =============================================================================
QLIB_PROVIDER_URI=<your_qlib_data_path>
CONDA_ENV_NAME=quantaalpha

# =============================================================================
# LLM API
# =============================================================================
OPENAI_API_KEY=<your_api_key>
OPENAI_BASE_URL=<your_base_url>

# Models
REASONING_MODEL=<your_model>
CHAT_MODEL=<your_model>

# Embedding (optional, for RAG)
# EMBEDDING_MODEL=text-embedding-v4
# EMBEDDING_API_KEY=<your_embedding_key>
# EMBEDDING_BASE_URL=<your_embedding_url>

# =============================================================================
# Global (usually no need to change)
# =============================================================================
USE_LOCAL=True
MAX_RETRY=5
RETRY_WAIT_SECONDS=5
FACTOR_MINING_TIMEOUT=999999
CHAT_MAX_TOKENS=4000
CHAT_TEMPERATURE=0.7

# =============================================================================
# Intraday integration (optional; required for intraday runner)
# =============================================================================

# ruogu token used by the local intraday evaluation framework
RUOGU_TOKEN=<your_ruogu_token>

# Either the workspace root containing fanalysis_intra/ or the package path itself
INTRADAY_FANALYSIS_ROOT=<path_to_workspace_or_fanalysis_intra>

# Evaluation window
INTRADAY_ANALYSIS_START=<yyyy-mm-dd>
INTRADAY_ANALYSIS_END=<yyyy-mm-dd>
INTRADAY_SLICE_START=<hh:mm:ss>
INTRADAY_SLICE_END=<hh:mm:ss>

# Evaluation config
INTRADAY_GROUP_NUM=10
INTRADAY_CONFIG_NAME=<analysis_config_name>
INTRADAY_FACTOR_CATEGORY=日内
INTRADAY_FACTOR_DESCRIPTION=QuantaAlpha intraday factor
INTRADAY_FACTOR_PREFIX=qa_intra
